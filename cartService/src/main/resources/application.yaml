eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

spring:
  application:
    name: Cart-Service
server:
  port: 8080
  data:
    mongodb:
      host: localhost
      port: 27017
      database: cart